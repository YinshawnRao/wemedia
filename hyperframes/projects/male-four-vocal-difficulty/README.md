# Male Four Vocal Difficulty

Status: fast local preview generated; placeholder for future HyperFrames refinement.

The first pass is a fast local preview. If the cut points and pitch notes are approved, migrate the timing and overlays into a production HyperFrames composition here.

Brief:

```text
data/briefs/male-four-vocal-difficulty.md
```

Source log:

```text
data/sources/experiment-011_sources.md
```

Current preview:

```text
sandbox/exports/experiment-011/male-four-vocal-difficulty-preview.mp4
```

Contact sheet:

```text
sandbox/exports/experiment-011/male-four-vocal-difficulty-contact-sheet.jpg
```

Refinement notes:

- Treat the concept as four different difficulty profiles instead of a hard all-time ranking.
- Keep `参考峰值` wording until pitch notes are verified by a formal pitch workflow.
- 陶喆段当前使用 `黑色柳丁`，不是 `普通朋友`，因为前者更符合“演唱难度代表作”的主题。
