# Leehom Wang TOP5 Vocal Difficulty

Status: fast local preview rendered.

The first pass uses Chrome-rendered PNG overlays plus ffmpeg concat. If the cut points and difficulty notes are approved, migrate the timing and overlays into a production HyperFrames composition here.

Brief:

```text
data/briefs/leehom-top5-vocal-difficulty.md
```

Source log:

```text
data/sources/experiment-014_sources.md
```

Current preview:

```text
sandbox/exports/experiment-014/leehom-top5-vocal-difficulty-preview.mp4
sandbox/exports/experiment-014/leehom-top5-vocal-difficulty-contact-sheet.jpg
```

Render helper:

```text
experiments/experiment-014/render_top5_preview.sh
```

If the cut points and notes are approved, migrate the same timings into a production HyperFrames composition under this project.
